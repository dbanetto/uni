int f(int x) {
    int y = -1;
    switch(x) {
        case 1: y = 1; break;
        case 2: y = 2; break;
        default: y = -1; break;
    }
    return y;
}

void main() {
    assert 1 == f(1);
    assert -1 == f(0);
}