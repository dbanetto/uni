int f(int[] x) {
    switch(x) {
        case []:
            return 0;
        case [1]:
            return -1;
    }
    return 10;
}

void main() {
    assert 0 == f([]);
    assert -1 == f([1]);
    assert 10 == f([3]);
    assert 10 == f([1,2,3]);
}
