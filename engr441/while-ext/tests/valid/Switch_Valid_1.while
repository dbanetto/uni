int f(int x) {
    switch(x) {
        case 1:
            return 0;
        case 2:
            return -1;
    }
    return 10;
}

void main() {
    assert 0 == f(1);
    assert -1 == f(2);
    assert 10 == f(3);
    assert 10 == f(-1);
}
