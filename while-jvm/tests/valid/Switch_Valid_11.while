int f(string x) {
  switch(x) {
    case "Red":
      return 0;
    case "Blue":
      return 1;
    case "Green":
      return 2;
 }
  return -1;
}

int main() {
    assert 0 == f("Red");
    assert 1 == f("Blue");
    assert 2 == f("Green");
    assert -1 == f("Dave");    
}